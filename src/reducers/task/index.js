

const initialState = {
  status: null,
  taskList: [],
}

const list = (state = initialState, action) => {
  switch (action.type) {
    default: {
      return {
        ...state
      };
    }
  }
}

export default list;
