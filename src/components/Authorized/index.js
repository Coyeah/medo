import react from 'React';

class Authorized extend React.Component {
  render() {
    const {
      key,
      roleMap,
    } = this.props;

    return (

    )
  }
}

export default Authorized;

/**
 * in   => roleKey & children & roleMap
 * out  => component
 */
